export class Product {
  constructor( no:number,name: string, price: number, stack:string) {
    this.no=no
    this.name = name;
    this.price = price;
    this.stack=stack
  }
  no: number;
  name: string;
  price: number;
  stack:string;
}
